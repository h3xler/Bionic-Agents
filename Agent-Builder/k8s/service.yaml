apiVersion: v1
kind: Service
metadata:
  name: agent-builder
  namespace: livekit
  labels:
    app: agent-builder
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: http
    protocol: TCP
  selector:
    app: agent-builder

