apiVersion: v1
kind: Service
metadata:
  name: agent-runtime-python
  namespace: livekit
  labels:
    app: agent-runtime-python
    component: runtime
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
  selector:
    app: agent-runtime-python

